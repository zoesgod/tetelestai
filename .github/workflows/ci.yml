name: Call Continuous Integration

on:
  push:
    branches:
      - main
      - develop_tombamento

permissions:
  contents: read
  id-token: write

jobs:
  horusec:
    uses: zoesgod/bygrace/.github/workflows/horusec.yml@main
  eslint:
    uses: zoesgod/bygrace/.github/workflows/eslint.yml@main

