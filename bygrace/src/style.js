import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#000',
      alignItems: 'center',
      justifyContent: 'center',
    },
    
    openscreen: {
      fontSize: 30,
      color: "white",
      fontWeight: "bold",
      textAlign: 'center',
    },
    
    subtitleopenscreen: {
      fontSize: 18,
      color: "white",
      textAlign: 'center',
    }
})